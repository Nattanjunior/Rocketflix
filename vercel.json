{
  "version": 2,
  "builds": [
    { "src": "front-end", "use": "@vercel/static" },
    { "src": "back-end/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/back-end/(.*)", "dest": "http://localhost:3000/$1" },
    { "src": "/(.*)", "dest": "front-end/$1" }
  ]
}
